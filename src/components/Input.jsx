import React from 'react';

const Input = (props) => {
  return (
    <div className='input-field'>
      <input type='text'></input>
      <button>Set</button>
    </div>
  );
}

export default Input;
